version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models:ro  # Mount models (faster dev)
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
